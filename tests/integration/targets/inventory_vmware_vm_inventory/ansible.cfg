[defaults]
inventory = test-config.vmware.yaml

[inventory]
enable_plugins = notmintest.not_a_real_collection.vmware_vm_inventory
cache = True
cache_plugin = jsonfile
cache_connection = inventory_cache

